# dependencies
node_modules/

# next.js
.next/
out/

# production
build/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (keep secrets local)
.env*
!.env.example
!.env.local.example

# typescript
*.tsbuildinfo

# editor
.vscode/
.DS_Store

# misc
build-output.txt
clerk-nextjs/
convex/_generated/
.vercel

# Capacitor Android (generated — rebuild via `npm run android:build`)
android/
# Keep the APK download folder tracked (but not the APK binary itself)
public/downloads/*.apk

# Signing keys (NEVER commit)
*.keystore
*.jks
.env*.local
