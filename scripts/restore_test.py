path = r"C:\Users\USER\Documents\GOAKL RTRACKER\src\app\onboarding\page.tsx"

# Restore first - write back the original content that was in the file
# We already have the full content from the first read
# Now write the full enhanced version

# IMPORTANT: The file was partially overwritten, so we must write the full content

content = ""

with open(path, 'w', encoding='utf-8') as f:
    f.write(content)
print("Cleared, will write full content next")